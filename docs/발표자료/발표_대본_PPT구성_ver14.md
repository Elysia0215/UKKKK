# UKKKK 데모데이 발표 대본 및 PPT 구성안 ver14

> ver14 동기화 메모: 2026-07-26 현재 코드와 `proposals.json`을 다시 계산해 작성했습니다. `필터 검증 통계`의 차트 드릴다운, 실제 최다 연도 계산, 원문 확인 대상의 정상 승인 흐름까지 반영합니다.

> 최종 점검 기준일: 2026-07-26  
> 발표 길이: 10분(600초)  
> 발표 원칙: **문제 가설 → 가상 사용자 → 데이터 → 판단 구조 → 새싹이와 함께 보는 제품 흐름 → 라이브 시연 → 확인 범위와 한계**

---

## 0. 이 문서의 사용법

이 문서는 단순 기능 소개서가 아니라, 실제 구현 상태에 맞춰 작성한 **발표자용 대본·PPT 제작 지시서·라이브 시연 큐시트**입니다.

- 슬라이드에는 결론과 숫자만 남기고, 세부 설명은 발표자가 말합니다.
- 구현되지 않았거나 검증되지 않은 효과를 완료된 사실처럼 말하지 않습니다.
- “AI가 결정한다”가 아니라 **데이터가 후보를 좁히고 담당자가 최종 판단한다**고 설명합니다.
- “실시간 API”라는 표현은 현재 화면의 필터·재계산 반응을 설명할 때만 사용합니다. 현재 시연 데이터는 수집·정제·캐시된 JSON을 중심으로 동작합니다.
- 공무원의 현행 업무 절차를 직접 조사한 결과가 아니므로, **확인된 AS-IS가 아니라 제품 설계를 위한 업무 가설**이라고 설명합니다.
- 김서현 주무관은 실제 인물이 아니라 기능 시연을 위해 만든 **가상 페르소나**입니다.
- 아래의 `검증된 사실`, `주의해서 말할 내용`, `시연 예상 결과`를 발표 전날 다시 대조합니다.

---

## 1. 발표에서 사용할 검증된 사실

### 1-1. 데이터 규모

| 데이터 | 현재 파일 기준 | 발표 표현 |
|---|---:|---|
| 상상대로 서울 시민 제안 | **824건** | 시민 정책 수요 |
| 국민신문고 민원 | **582건** | 현장 고충 신호 |
| 몽땅정보통 공식 사업 | **322건** | 기존 정책 공급 |
| 뉴스 기사 | **1,145건** | 사회 이슈·보도 신호 |
| 자치구 공공통계 | **25개 자치구** | 출생·보육 지역 지표 |
| 정책 갭 진단 | **57개 문제 군집** | 의사결정 후보 단위 |

### 1-2. 시민 제안 데이터 품질

| 항목 | 현재 값 | 해석 |
|---|---:|---|
| 전체 제안 | 824건 | 분석 모수 |
| 분석·정책 대조 가능 | **730건** | `connection_status=reviewable` |
| 분석 범위 밖 | **78건** | 출산·양육 분석에 직접 포함하지 않는 레코드 |
| 원문 확인 필요 | **16건** | `connection_status=source_missing`; 짧다는 이유만으로 결측 확정하지 않음 |
| 원본 특정 자치구 | **20건** | 원 데이터에 자치구가 선택된 레코드 |
| 본문 특정 자치구 후보 | **49건** | 원본은 미상이지만 본문에 25개 자치구명이 직접 등장 |
| 서울시 전체 명시 | **5건** | `서울시 전체·서울 전역·전 자치구` 등이 직접 등장 |
| 지역 범위 미확인 | **750건** | 서울시 전체 요구와 판별 불가가 섞여 있어 추가 단정 금지 |
| 답변 완료 | 64건 | 화면 데이터 기준 |
| 미답변 | 760건 | 화면 데이터 기준 |

### 1-3. 정책 분류와 R&R 구조

- 제안은 **8대 정책 대분류 → 중분류 → 세분류**로 탐색합니다.
- 상단 첫 번째 선택기는 조직명이 아니라 **8대 정책 대분류 담당군**입니다.
- 상단 두 번째 선택기는 원본 업무분장에 근거한 **실제 R&R 팀**입니다.
- 전체 담당군에서 실제 팀을 선택하면 그 팀이 **1·2·3순위 어디에든 포함된 안건**을 찾습니다.
- 특정 대분류 담당군을 먼저 선택한 뒤 실제 팀을 고르면 그 대분류 안에서 **해당 팀이 주관 또는 협조 후보로 포함된 안건**을 찾습니다.
- 현재 화면의 R&R 선택기에 실제 등장하는 팀명은 **11종**이며, 각 제안에는 1순위 주관부서와 2·3순위 협조부서 후보가 저장됩니다.
- R&R은 공식 배정 확정이 아니라 **업무분장 근거를 보여주는 검토 후보 순위**입니다.

### 1-4. 발표에서 과장하지 않을 내용

- “짧은 원문은 모두 결측이다” → 사용 금지.  
  **“원문 연결 실패 16건만 확인 대상으로 두고, 원문과 동일한 짧은 제안은 정상 원문으로 승인한다”**고 말합니다.
- “18개 부서를 완벽히 자동 배정했다” → 사용 금지.  
  **“원본 업무분장 기반 1·2·3순위 R&R 후보를 제시한다”**고 말합니다.
- “업무 시간이 15분에서 15초로 70% 감소했다” → 계산도 틀리고 검증도 안 됨.  
  **“업무시간 단축과 추천 정확도는 검증하지 않아 성과 수치로 제시하지 않는다”**고 말합니다.
- “진짜 HWP/PDF 바이너리를 생성한다” → 사용 금지.  
  현재 HWP는 `.hwp.txt`, PDF 요약은 화면 미리보기·인쇄 흐름, 원자료는 CSV입니다.
- “프로덕션 최적화가 100% 완료됐다” → 사용 금지.  
  현재 빌드는 통과하지만, 대용량 데이터 청크 최적화는 후속 과제입니다.
- “AI가 정책을 결정한다” → 사용 금지.  
  **“AI·규칙 기반 초안을 담당자가 검토·수정·승인한다”**고 말합니다.
- 실제 기관의 업무 절차와 부담을 조사·검증한 것처럼 표현하지 않습니다.  
  **“제품 기능을 설명하기 위해 설정한 검토 흐름”**이라고 말합니다.

---

## 2. 10분 발표 전체 구성

| 파트 | 슬라이드 | 제목 | 시간 |
|---|---:|---|---:|
| 오프닝 | 0 | 시민의 목소리를 행정의 다음 행동으로 | 15초 |
| 전환 배경 | 1 | 과제에서 발견한 연결 공백 | 20초 |
| 제품 방향 | 2 | 상상대로 활용을 돕는 백오피스 | 20초 |
| 문제·사용자 | 3 | 페르소나: 신규 정책담당 김서현 주무관 | 30초 |
| 문제·사용자 | 4 | 검증할 업무 가설: 정책 수요 검토는 어디에서 단절되는가 | 30초 |
| 가치 제안 | 5 | TO-BE: 한 화면에서 근거와 R&R까지 | 20초 |
| 데이터 | 6 | 5원 데이터 융합 구조 | 35초 |
| 분류·R&R | 7 | 두 단계 담당 범위 선택 | 45초 |
| 의사결정 | 8 | 57개 문제 군집과 우선순위 진단 | 40초 |
| 화면 소개 | 9 | 수요를 발견하는 3개 화면 | 25초 |
| 화면 소개 | 10 | 공급과 지역을 대조하는 3개 화면 | 25초 |
| 화면 소개 | 11 | 근거·판단·품질·필터 검증을 잇는 4개 화면 | 30초 |
| 제품 | 12 | 발견에서 조치까지 End-to-End | 45초 |
| 지원 기능 | 13 | 새싹이와 보고서 내보내기 | 35초 |
| 데이터 품질 | 14 | 모르는 값은 모른다고 표시한다 | 35초 |
| 시연 도입 | 15 | 오늘의 시연 과업 | 25초 |
| 라이브 데모 | 16 | 보육·돌봄 인프라 → 가족건강팀 20건 | 105초 |
| 검증 | 17 | 현재 확인된 것과 아직 검증할 것 | 30초 |
| 로드맵 | 18 | 운영형 정책 의사결정 도구로 | 30초 |
| 엔딩 | 19 | 시민 수요를 다음 행동으로 | 15초 |
| **합계** |  |  | **600초** |

---

# 3. 슬라이드별 PPT 구성 및 상세 대본

## Slide 0. 시민의 목소리를 행정의 다음 행동으로

### PPT 화면

- 레트로 오피스 창 형태의 흰색 표지.
- 중앙: `UKKKK`와 `2차 발표 시작` 버튼.
- 부제: `Unified Key-Knowledge Kit for Kid`
- 하단 좌측: 손을 흔들며 발표 여정을 시작하는 새싹이.
- 실제 말풍선: **“안녕하세요! 오늘은 제가 유케이의 기능을 안내할게요.”**

### 발표 대본 — 15초

> 안녕하십니까. 저희는 4조 UKKKK입니다. 이름이 길어서 발표에서는 편의상 **‘유케이’**라고 부르겠습니다.  
> 화면별 기능을 안내하는 오피스 길잡이 **새싹이**도 오늘의 여정을 함께합니다.

---

## Slide 1. 문제 정의: 상상대로 데이터의 연결 공백

### PPT 화면

- 헤드라인: **“답변 상태·반복 수요·담당팀 후보·정책 근거가 한 흐름으로 연결되지 않았습니다.”**
- 상단 맥락: **“상상대로 서울 실제 데이터에서 확인한 문제”**
- 하단 원래 과제: `출산·양육 민원과 데이터 수집 → 정책 수요 도출`
- 하단 좌측: 돋보기로 데이터를 살펴보는 새싹이.
- 새싹이 말풍선: **“답변 상태와 반복 수요, 담당 후보와 정책 근거가 따로 보였어요.”**

### 발표 대본 — 20초

> 원래 과제는 출산·양육 관련 민원과 데이터를 수집해 정책 수요를 도출하는 것이었습니다. 그런데 실제 ‘상상대로 서울’ 데이터를 확인하면서, 시민 제안의 답변 상태와 반복 수요, 실제 담당팀 후보, 기존 정책 근거가 한 흐름으로 연결되어 있지 않다는 점에 주목했습니다.

---

## Slide 2. 상상대로 활용을 돕는 백오피스

### PPT 화면

- 헤드라인: **“상상대로를 대체하지 않고, 정책 검토와 후속 조치를 연결합니다.”**
- 연결 구조: `시민 제안·민원·정책·뉴스·통계 → 담당 범위·판단 근거 → 다음 조치`
- 하단 핵심 문장: **“기존 플랫폼의 활용을 돕는 행정 의사결정 지원 백오피스”**
- 하단 좌측: 길을 안내하는 새싹이.
- 새싹이 말풍선: **“흩어진 수요와 근거를 다음 조치까지 이어드릴게요.”**

### 발표 대본 — 20초

> 그래서 저희는 상상대로 서울을 대체하는 서비스가 아니라, **시민 제안을 정책 검토와 후속 조치로 연결해 플랫폼의 활용을 돕는 백오피스**를 만들었습니다.  
> 유케이는 시민 제안·민원·정책·뉴스·통계를 통합하고, 시민 수요를 담당 범위와 판단 근거, 다음 조치로 연결하는 행정 의사결정 지원 대시보드입니다.

---

## Slide 3. 페르소나: 신규 정책담당 김서현 주무관

### 페르소나

| 항목 | 내용 |
|---|---|
| 이름 | **김서현 주무관(가상 페르소나)** |
| 설정 | 서울시 가족건강팀 정책업무 배치 6개월 차로 가정 |
| 가정한 과업 | 시민 제안 검토, 소관·협조 후보 확인, 기존 사업 대조, 보고자료 작성 |
| 시연 상황 | 월요일 오전, 출산·양육 제안 중 우선 검토 안건을 선별하는 상황을 가정 |
| 제품이 다루는 불편 | 데이터 탐색, 담당 후보 확인, 원문·지역 범위 검토가 분리될 때 생길 수 있는 연결 부담 |
| 목표 | “내가 볼 안건을 빠르게 좁히고, 왜 중요한지 근거를 확인한 뒤 보고서 초안까지 만든다.” |
| 성공 기준 | 범위 선택 오류 없이 관련 안건만 확인, 주관·협조 후보 구분, 원문·정책·민원 근거 확인, 결과 내보내기 |

### PPT 화면

- 상단 질문: **“월요일 오전, 무엇부터 봐야 할까요?”**
- 좌측 카드: 가정한 상황 — 출산·양육 정책 담당, 여러 출처 확인, 검토 우선순위 필요.
- 우측 카드: 필요한 지원 — 내 담당 범위 설정, 반복 수요·근거 확인, 보고자료 작성.
- 하단 좌측: 돋보기 새싹이.
- 실제 말풍선: **“실제 인물이 아닌 기능 시연용 가상 페르소나예요.”**

> 위 페르소나 표는 발표자 참고용 상세 설정입니다. 실제 슬라이드에는 표 전체를 넣지 않고, 현재 PPT와 같이 `상황 → 필요한 지원` 두 카드로 요약합니다.

### 발표 대본 — 30초

> 김서현 주무관은 실제 인물이 아니라 제품의 기능과 시연 과업을 구체화하기 위해 설정한 가상 페르소나입니다.  
> 시민 제안을 검토하고 담당 후보와 기존 정책 근거를 대조한 뒤 보고자료를 만든다는 과업을 가정했습니다.  
> 실제 상상대로 서울에서는 답변 상태와 반복 수요, 담당 후보와 정책 근거가 서로 분리되어 있어 검토 대상과 다음 조치를 한 화면에서 직관적으로 파악하기 어렵습니다. 가상 페르소나는 이 제품 기능과 시연 흐름을 구체적으로 보여주기 위한 설정입니다.  
> 그래서 사용자의 질문을 하나로 정의했습니다. **“이 안건은 누가, 왜, 얼마나 먼저 봐야 하는가?”**

---

## Slide 4. 검증할 업무 가설: 정책 수요 검토는 어디에서 단절되는가

### PPT 화면

화면 중앙 문구:

**“데이터는 있었지만, 수요와 책임·근거·조치가 끊겨 있었습니다.”**

- 화면은 중앙 문제 문장과 하단 새싹이 안내로 구성.
- 하단 좌측: 자를 든 새싹이.
- 실제 말풍선: **“실제 기관별 업무 절차를 대표한다고 단정하지 않아요.”**

### 발표 대본 — 35초

> 상상대로 서울에는 시민 제안이 축적되어 있지만, 제안만으로는 **어떤 수요를 먼저 살펴볼지, 어느 정책 영역과 실제 담당 후보가 연결되는지, 판단 근거와 다음 조치가 무엇인지**를 한 흐름에서 확인하기 어렵습니다.  
> 저희는 시민 수요와 정책·민원·뉴스·통계, 그리고 **주관·협조부서 후보가 제시된 이유**를 연결해 이 단절을 보완하는 것을 제품이 다룰 문제로 정의했습니다.  
> 화면의 누락·혼선·근거 단절은 실제 기관의 확정된 업무 절차가 아니라, 수집 데이터와 제품 구조를 바탕으로 설정한 검토 위험입니다.

---

## Slide 5. TO-BE: 한 화면에서 근거와 R&R까지

### PPT 화면

- 좌측 `입력`: 시민 제안·민원, 정책·뉴스, 자치구 통계.
- 중앙 화살표.
- 우측 `출력`: 반복 수요·Gap, R&R 후보·원문, 맞춤 보고서·로그.
- 하단 좌측: 데이터를 정리하는 새싹이.
- 실제 말풍선: **“후보와 근거를 연결하고 최종 결정은 사람이 해요.”**

### 발표 대본 — 25초

> 화면의 왼쪽은 유케이가 받는 입력입니다. 시민 제안과 민원, 기존 정책과 뉴스, 자치구 통계를 한데 모읍니다.  
> 오른쪽은 그 데이터를 연결해 만든 출력입니다. 반복되는 수요와 정책 Gap, 주관·협조 R&R 후보, 원문 근거, 맞춤 보고서와 검토 로그를 제공합니다.  
> 즉, 이 화면은 실제 조작 순서를 설명하는 페이지가 아니라 **흩어진 입력을 검토 가능한 출력으로 바꾸는 제품의 역할**을 보여줍니다. 최종 판단과 승인은 담당자에게 남겨 둡니다.

---

## Slide 6. 5원 데이터 융합 구조

### PPT 화면

5개 입력 노드:

1. 상상대로 시민 제안 **824**
2. 국민신문고 민원 **582**
3. 몽땅정보통 공식 정책 **322**
4. 뉴스 **1,145**
5. 25개 자치구 공공통계

가운데 처리:

`텍스트 정규화 → 계층 분류 → KR-SBERT 의미 분류·군집 → 뉴스 토픽 파싱 → 지역·R&R 파싱`

공통 스키마:

`제안ID · 제목/본문 · 등록일 · 공감/댓글 · 답변상태 · 지역 · 대/중/소분류 · R&R 1·2·3순위 · 원문 URL`

전처리 원칙:

- 제안 ID·날짜·답변 상태·URL 형식 통일
- 조사·어미·불용어 제거와 도메인 유사어 통합
- 제목을 본문으로 복제하지 않고 원문 미연결 상태를 별도 표시
- 원본 미상은 결측치로 일괄 처리하지 않고 `본문 특정 자치구 후보 / 서울시 전체 명시 / 지역 범위 미확인`으로 구분
- 특정 자치구 후보는 본문에 25개 자치구명이 직접 등장하는 경우에만 제시
- 정책 대분류와 실제 조직명을 분리하고 원본 업무분장으로 R&R 후보 연결

하단 문구:

**“수요만 보지 않고, 현장·공급·사회 신호·지역 조건을 함께 본다.”**

### 발표 대본 — 40초

> 데이터는 다섯 출처를 결합했습니다. 상상대로 시민 제안 824건은 정책 수요, 국민신문고 582건은 현장 고충, 몽땅정보통 322개 사업은 기존 공급, 뉴스 1,145건은 사회적 이슈, 자치구 통계는 지역 조건을 보여줍니다.  
> 현재 데모는 이 데이터를 미리 수집·정제해 캐시한 구조로 안정적으로 재현됩니다.  
> 서로 다른 출처는 제안 ID와 본문, 날짜, 공감·답변 상태, 지역, 정책 계층, R&R 순위, 원문 URL을 가진 공통 스키마로 맞췄습니다. 이후 불용어와 유사어를 정규화하고, 본문과 지역이 없는 값은 임의로 채우지 않은 채 별도 상태로 관리합니다.  
> 텍스트는 한 모델이 한 번에 결정하지 않습니다. 먼저 조사·접미사와 불용어를 정리하고 유사어를 통합한 뒤, 규칙 기반으로 8대·중·소분류와 신뢰도를 만듭니다. KR-SBERT는 의미상 가까운 카테고리와 유사 제안 군집을 찾고, 뉴스는 규칙 분류 뒤 기존 카테고리·수집 분류·NMF 토픽 순으로 보완합니다. 마지막으로 본문 자치구와 원본 업무분장을 각각 파싱해 지역 후보와 R&R 후보를 연결합니다.  
> 즉, 제안 건수 하나만으로 판단하지 않고 **수요·현장·공급·사회·지역 근거를 함께 보는 구조**입니다.

### 발표자 주의

- “모든 데이터가 발표 순간 API로 새로 수집된다”고 말하지 않습니다.
- `실시간`은 필터와 화면 재계산, 사용자 조작 반응을 설명할 때 사용합니다.

---

## Slide 7. 두 단계 담당 범위 선택

### PPT 화면

```text
1단계 정책 담당군
전체 또는 8대 정책 대분류
        ↓
2단계 실제 R&R 팀
원본 업무분장 기반 팀명
```

두 가지 동작을 나란히 표시:

| 선택 방식 | 검색 의미 |
|---|---|
| 전체 담당군 + 실제 팀 | 해당 팀이 1·2·3순위 어디에든 포함된 안건 |
| 특정 담당군 + 실제 팀 | 해당 대분류에서 그 팀이 주관 또는 협조 후보로 포함된 안건 |

우측에 제안 카드:

- `[주관부서] 1순위`
- `[협조부서] 2순위`
- `[협조부서] 3순위`

하단 모델 요약:

`8대 정책 분류 → 업무분장 가중치 → R&R 후보 1·2·3순위`

### 발표 대본 — 45초

> 앞서 소개한 오피스 길잡이 새싹이가 첫 번째로 안내할 기능은 상단의 두 단계 담당 범위 선택입니다.  
> 첫 번째는 조직이 아니라 8대 **정책 대분류 담당군**입니다. 두 번째가 원본 업무분장에 근거한 **실제 R&R 팀**입니다.  
> 이때 별도의 감독학습 모델이 팀을 확정하는 것은 아닙니다. 제안 분류와 원본 업무분장 일치 여부, 본문의 업무 키워드, 기존 담당 후보를 점수화해 R&R 후보 순위를 만듭니다.  
> 전체 담당군에서 팀을 고르면 그 팀이 주관 또는 협조로 포함된 모든 안건을 찾습니다. 특정 대분류를 먼저 고르면 동일한 R&R 기준을 그 정책 범위 안에서 적용해 관련 안건만 보여줍니다.  
> 따라서 가족지원팀처럼 여러 분야에 협조로 참여하는 팀도 중복을 숨기지 않으면서, 사용자는 지금 ‘관련 안건 전체’를 보는지 ‘주관 안건’을 보는지 구분할 수 있습니다.

### 캡처 레퍼런스 — 2개 화면

1. `수요 현황 종합`에서 `전체 대분류 담당군 + 전체 실제 R&R 팀` 상태의 상단 두 드롭다운을 캡처합니다.
2. 같은 화면에서 `보육·돌봄 인프라 → 가족건강팀`을 선택하고, 두 선택값과 아래 범위 안내 카드가 함께 보이게 캡처합니다.
3. PPT에는 전·후 화면을 좌우 1:1로 놓고 화살표로 연결합니다.

### 기술·정책 표현

- “자동 배정”보다 **“R&R 후보 랭킹”**이라고 말합니다.
- 현재 R&R 랭킹은 카테고리·업무 키워드·기존 담당 후보를 결합한 **규칙·가중치 기반 모델**입니다.
- 1순위는 주관 후보, 2·3순위는 협조 후보입니다.
- 최종 배정 책임은 담당자에게 있습니다.

---

## Slide 8. 57개 문제 군집과 우선순위 진단

### PPT 화면

중앙 수식:

`우선순위 = 수요 + 정책 공백 + 시급성 + 실행성 + 근거 신뢰도`

기본 가중치:

| 수요 | 공백 | 시급성 | 실행성 | 근거 신뢰도 |
|---:|---:|---:|---:|---:|
| 30% | 25% | 25% | 10% | 10% |

대안 모델:

- 시민수요 강조: `40 / 20 / 20 / 10 / 10`
- 지역형평성 강조: `20 / 40 / 20 / 10 / 10`

오른쪽:

- X축: 정책 공백
- Y축: 시급성
- 버블 크기: 수요
- 전체 범위: 57개 문제 군집
- 대분류 담당군·실제 R&R 팀 선택 시 해당 범위의 군집·점수·근거 패널로 재구성

### 발표 대본 — 40초

> 개별 제안만 읽으면 같은 요구가 여러 표현으로 흩어집니다. 유케이는 전체 범위의 제안을 57개 문제 군집으로 묶고, 수요·정책 공백·시급성·실행성·근거 신뢰도의 다섯 축으로 비교합니다.  
> 이 진단도 고정된 전체 결과만 보여주는 것이 아닙니다. 상단에서 대분류 담당군이나 실제 R&R 팀을 선택하면 그 업무 범위에 포함되는 문제 군집만 남고, 우선점수와 진단 매트릭스, 상세 근거 패널이 같은 기준으로 다시 구성됩니다.  
> 기본 모델은 수요 30%, 공백과 시급성 각 25%, 실행성과 신뢰도 각 10%입니다. 사용자는 목적에 따라 시민수요 강조형과 지역형평성 강조형으로 가중치를 바꿔 순위 변화를 볼 수 있습니다.  
> 이 점수는 정답이 아니라 **검토 순서를 설명하는 의사결정 보조 지표**입니다.

### 캡처 레퍼런스 — 2개 화면

1. 왼쪽 메뉴 `정책 갭 진단`: 57개 군집 표, 우선점수, 추천 액션 열이 보이는 매트릭스 화면.
2. 군집 1개 클릭 후: 5축 점수, 주관 1순위·협조 2·3순위, 관련 정책·민원·뉴스가 보이는 상세 패널.
3. PPT 배치는 매트릭스 55%, 상세 패널 45%를 권장합니다.

### PPT 각주

`가중치 모델의 학술 근거 상세는 Appendix와 논문근거_실증흐름.md에서 제시`

---

## Slide 9. 수요를 발견하는 3개 화면

### PPT 화면

- 실제 화면 캡처 3개: `수요 현황 종합` / `시민 목소리 분석` / `긴급 민원 처리`
- 각 화면 아래 한 줄:
  - 수요 현황 종합: `담당 범위의 KPI·답변 분포·데이터 품질 확인`
  - 시민 목소리 분석: `반복 키워드·감정·이슈 흐름 확인`
  - 긴급 민원 처리: `고공감·미답변 안건의 원문과 담당 후보 확인`
- 공통 안내: **“화면 하단 새싹이를 누르면 현재 페이지와 선택한 담당 범위에 맞춰 사용 순서를 안내합니다.”**

### 발표 대본 — 25초

> 먼저 수요를 발견하는 세 화면입니다. 수요 현황 종합에서 선택 범위의 KPI와 답변 분포를 보고, 시민 목소리 분석에서 반복 키워드와 이슈 흐름을 확인합니다. 긴급 민원 처리에서는 고공감·미답변 안건의 원문과 주관·협조부서 후보가 제시된 이유를 함께 봅니다.  
> 어느 화면에서든 하단의 새싹이를 누르면 현재 페이지와 선택한 정책 담당군·실제 팀에 맞춰 다음 확인 순서를 안내합니다.

### 캡처 레퍼런스 — 3개 화면

1. `수요 현황 종합`: KPI 카드와 실제 팀별 제안 수·답변율 카드.
2. `시민 목소리 분석`: 상단 키워드 칩과 시계열·이슈 차트.
3. `긴급 민원 처리`: 긴급 필터, 연도 필터, 제안 카드 1개.
4. 세 화면 모두 `보육·돌봄 인프라 → 가족건강팀` 상태로 맞추고 같은 크기의 3열로 배치합니다.

---

## Slide 10. 공급과 지역을 대조하는 3개 화면

### PPT 화면

- 실제 화면 캡처 3개: `현행 정책 검색` / `정책 사각지대 탐색` / `자치구 통계 비교`
- 각 화면 아래 한 줄:
  - 현행 정책 검색: `322개 기존 사업을 검색하고 제안과 대조`
  - 정책 사각지대 탐색: `반복 수요와 직접 대응 정책의 공백 확인`
  - 자치구 통계 비교: `지역 지표와 시민 수요의 방향 비교`

### 발표 대본 — 25초

> 다음은 공급과 지역 조건을 대조하는 세 화면입니다. 현행 정책 검색에서 322개 사업 중 직접 연결되는 정책을 찾고, 정책 사각지대 탐색에서 반복 수요에 비해 대응 정책이 부족한 지점을 확인합니다. 자치구 통계 비교에서는 특정 지역 요구일 때 실제 지역 지표가 같은 방향을 보이는지 대조합니다.

### 캡처 레퍼런스 — 3개 화면

1. `현행 정책 검색`: 정책 제목·대상·신청 정보가 보이는 목록 2개.
2. `정책 사각지대 탐색`: 군집 카드 또는 버블과 선택 상세가 함께 보이는 영역.
3. `자치구 통계 비교`: 서울 지도와 선택 지표 카드가 함께 보이는 영역.
4. 3열 배치를 사용하되 지도 화면은 다른 캡처보다 폭을 10% 넓힙니다.

---

## Slide 11. 근거·판단·품질·필터 검증을 잇는 4개 화면

### PPT 화면

- 실제 화면 캡처 4개: `통합 정책 근거 분석` / `정책 갭 진단` / `결측치 복원 & 로그` / `필터 검증 통계`
- 각 화면 아래 한 줄:
  - 통합 정책 근거 분석: `시민제안·민원·정책·뉴스·통계 근거를 한 화면에서 교차 확인`
  - 정책 갭 진단: `문제 군집·5축 우선순위·담당부서 매칭 이유 확인`
  - 결측치 복원 & 로그: `지역 범위 후보·원문 연결 상태·수정 이력 관리`
  - 필터 검증 통계: `분류·연도·답변·R&R 분포와 클릭 가능한 제안 목록 검증`
- 하단 주의: **“점수와 담당 후보는 검토를 돕는 값이며 최종 결정은 담당자가 합니다.”**

### 발표 대본 — 30초

> 마지막 네 화면이 판단의 근거와 데이터 품질, 필터 적용 결과를 연결합니다. 통합 정책 근거 분석에서는 시민제안·민원·정책·뉴스·통계를 한 화면에서 교차 확인하고 출처까지 추적합니다. 정책 갭 진단에서는 문제 군집과 다섯 축 우선순위, 담당부서 매칭 이유를 검토합니다. 결측치 복원과 로그에서는 지역 범위 후보, 원문 연결 상태, 담당자의 수정 이력을 남깁니다.  
> 마지막 `필터 검증 통계`에서는 상단에서 선택한 대분류와 R&R 팀이 실제 제안 목록과 집계에 동일하게 적용됐는지 확인합니다. 분류·연도·답변·관련 팀 차트의 막대나 원형 조각을 누르면 해당 제안 ID와 제목, 본문, 공감·댓글, 공식 URL까지 드릴다운됩니다. 원문 연결 실패 후보는 원문과 실제 내용이 같으면 정상 원문으로 승인하고, 실패·불일치만 검토 대상으로 남깁니다.  
> 네 화면 모두 자동 결정을 내리는 것이 아니라 담당자가 검증할 근거를 모아주는 단계입니다.

### 캡처 레퍼런스 — 4개 화면

1. `통합 정책 근거 분석`: 상단 5개 관점 카드와 `제안·민원·뉴스 근거 비교` 본문.
2. `정책 갭 진단`: 5축 점수와 R&R 후보 상세.
3. `결측치 복원 & 로그`: 지역 범위 상태와 원문·수정 이력이 보이는 카드.
4. `필터 검증 통계`: 현재 표시 제안 수와 연도 분포가 보이는 기본 화면 1개 + `원문 확인 필요` 막대를 눌러 제안 목록과 `원문과 동일·정상 승인` 버튼이 보이는 확대 화면 1개 중 하나를 선택.
5. PPT는 2×2 그리드로 배치하고, 각 캡처 위에 왼쪽 메뉴의 실제 탭 이름을 표시합니다.

---

## Slide 12. 발견에서 조치까지 End-to-End 사용자 플로우

### PPT 화면

```text
[1] 오늘의 업무 범위 설정
  정책 담당군 + 실제 R&R 팀
           ↓
[2] 수요 신호 발견
  KPI + 키워드 + 긴급/반복 제안
           ↓
[3] 원문과 책임 확인
  상세 본문 + 1·2·3순위 + 공식 URL
           ↓
[4] 공급·근거 대조
  기존 정책 + 민원 + 뉴스 + 자치구 통계
           ↓
[5] 갭과 조치 검토
  우선순위 + 추천 액션 + 담당자 수정
           ↓
[6] 결과 전달
  승인 로그 + HWP 텍스트/PDF 요약/CSV
```

### 발표 대본 — 45초

> 새싹이가 안내하는 제품 시연 흐름은 여섯 장면으로 구성됩니다.  
> 먼저 정책 담당군과 실제 팀으로 오늘 볼 업무를 정합니다. 다음으로 KPI와 키워드, 긴급 안건에서 신호를 찾습니다. 관심 안건은 원문과 주관·협조 후보, 공식 URL로 검증합니다. 이어서 기존 사업과 민원·뉴스·자치구 통계를 대조합니다.  
> 정책 공백이 확인되면 갭 점수와 추천 조치를 보되, 담당자가 문구를 수정하고 승인합니다. 마지막으로 선택한 범위가 반영된 보고서나 CSV를 내보냅니다.  
> 이 흐름의 핵심은 **필터 조건과 판단 근거가 다음 화면까지 이어진다는 것**입니다.

### 캡처 레퍼런스 — 1개 화면 + 흐름 도식

- 실제 캡처는 `정책 갭 진단` 상세 화면 1개만 사용합니다.
- 제안 원문, 유사 정책, R&R 주관·협조 후보, 답변 초안 동작이 최대한 함께 보이는 상태를 캡처합니다.
- 캡처 주변에 `범위 선택 → 수요 발견 → 정책·지역 대조 → 갭 판단 → 검토·승인 → 보고서`를 도형으로 연결합니다.
- 작은 캡처 6개를 나열하지 않습니다.

---

## Slide 13. 새싹이와 보고서 내보내기

### PPT 화면

좌측 `새싹이 오피스 길잡이`:

- 현재 10개 화면별 목적·기본 단계·고급 팁
- 자치구 메뉴의 3개 하위 화면별 안내
- 정책 담당군 선택 시 대분류 흐름
- 실제 팀 선택 시 팀 기준 흐름

우측 `보고서 다운로드`:

- 상단에서 선택한 대분류 담당군·실제 R&R 팀 조합을 분석 범위로 반영
- 전체/대분류/실제 팀 중 어떤 기준을 선택해도 해당 범위의 수치·본문·추천 섹션으로 재구성
- 카테고리·지역 등 현재 필터 조건까지 함께 반영
- 상세형 / 간부 브리핑 / 외부 공유용
- HWP 텍스트 / PDF 요약 / Excel CSV
- 6개 섹션 선택형 미리보기

### 발표 대본 — 35초

> 새싹이 오피스 길잡이는 사용자가 현재 화면에서 무엇을 확인할 수 있는지 안내합니다. 정책 담당군을 선택하면 대분류 기준으로, 실제 팀을 선택하면 팀 기준으로 안내 내용과 화면 흐름을 바꿉니다.  
> 이 선택 범위는 화면 조회에서 끝나지 않고 보고서까지 그대로 이어집니다. 전체 범위를 선택하면 전체 R&R 통합 보고서가, 특정 대분류 담당군을 선택하면 해당 정책 분야 보고서가, 실제 팀까지 선택하면 그 팀의 주관·협조 대상만 집계한 맞춤 보고서가 생성됩니다.  
> 검토 결과는 우측 보고서 다운로드에서 상세형, 간부 브리핑, 외부 공유용으로 구성할 수 있습니다. 현재 구현은 HWP용 텍스트, PDF 요약 미리보기, CSV 원자료를 제공하며, 현재 선택한 담당 범위와 카테고리·지역 조건이 보고서의 수치, 본문, 추천 섹션에 함께 반영됩니다.

### 캡처 레퍼런스 — 2개 화면

1. `통합 정책 근거 분석`에서 오른쪽 하단 새싹이를 열고, 화면명·선택 범위·다음 행동이 보이게 캡처합니다.
2. 우측 상단 `보고서 다운로드`를 열고, 보고서 유형·파일 형식·추천 섹션·미리보기가 함께 보이게 캡처합니다.
3. 배치는 새싹이 35%, 보고서 빌더 65%입니다.

---

## Slide 14. 모르는 값은 모른다고 표시한다

### PPT 화면

지역 범위 4단계:

- 원본 특정 자치구 **20건**
- 본문 특정 자치구 후보 **49건**
- 서울시 전체 명시 **5건**
- 지역 범위 미확인 **750건**

별도 품질 지표:

- 원문 연결 확인 필요 **16 / 824**

품질 처리 원칙:

1. 제목을 상세 본문처럼 반복하지 않음
2. 원본 `미상`을 결측치 또는 `서울시 전체`로 일괄 변환하지 않음
3. 본문에 자치구명이 직접 등장할 때만 복원 후보로 표시
4. 체크한 결과만 반영 후보로 기록
5. 오매칭·복원·승인 이력을 통합 로그에 저장

### 발표 대본 — 35초

> 저희는 원본의 자치구 `미상`을 모두 결측치로 보거나 특정 구로 강제 복원하지 않습니다. 824건 중 원본에서 자치구가 선택된 것은 20건입니다. 나머지 중 본문에 25개 자치구명이 직접 등장하는 복원 후보는 49건, 서울시 전체 범위를 명시한 제안은 5건이며, 750건은 서울시 전체 요구와 판별 불가가 섞인 `지역 범위 미확인`으로 남깁니다.  
> 원문 연결에 실패한 레코드는 16건입니다. 본문 길이가 짧다는 이유만으로 결측 처리하지 않고, 원문과 저장 내용이 같으면 정상 원문으로 승인하며 수집 실패·불일치만 검토 대상으로 남깁니다.  
> 복원 후보는 담당자가 원문을 펼쳐 검토하고 선택한 결과만 반영 이력으로 남깁니다.  
> 데이터 품질을 숨기지 않는 것이 공공 의사결정 시스템의 신뢰라고 판단했습니다.

### 캡처 레퍼런스 — 2개 화면

1. `결측치 복원 & 로그`: 원본 특정 구·본문 특정 구 후보·서울시 전체·미확인 집계가 보이는 화면.
2. 같은 탭의 상세 사례: 원본 값, 추정 구, 근거 키워드, 점수, 승인·오류 신고 동작.
3. 배치는 집계 40%, 상세 사례 60%이며 캡션에 `원본 미상 804건을 모두 특정 구 결측으로 보지 않음`을 넣습니다.

---

## Slide 15. 라이브 데모 전에 볼 흐름

### PPT 화면

`빠른 10개 화면 투어 + 핵심 3개 화면 깊은 시연`

과업:

> “보육·돌봄 인프라 분야에서 가족건강팀 관련 제안을 찾고, 수요·공급·지역·책임 근거를 확인한 뒤 보고자료를 만든다.”

시연 성공 조건:

1. `보육·돌봄 인프라` 선택 후 **179건**
2. `가족건강팀` 선택 후 **20건**
3. 좌측 10개 화면을 한 번씩 열어 범위 연동 확인
4. 정책 갭 진단에서 군집·우선순위·담당부서 매칭 이유 확인
5. 긴급 민원에서 원문·공식 URL·주관·협조 후보 확인
6. 새싹이 안내와 보고서 모달에 현재 범위 반영

### 발표 대본 — 25초

> 김서현 주무관의 화면에서 먼저 보육·돌봄 인프라 담당군을 선택해 179건을 확인하고, 실제 가족건강팀을 선택해 20건으로 좁히겠습니다.  
> 이어서 열 개 메뉴를 한 번씩 열어 같은 담당 범위가 유지되는지 빠르게 확인합니다. `통합 정책 근거 분석`에서는 제안·민원·정책·뉴스·통계의 연결 근거를 확인하고, `필터 검증 통계`에서는 같은 20건이 분류·연도·답변·R&R 집계에도 동일하게 적용됐는지 확인합니다. 그다음 갭 진단, **제안 원문과 주관·협조부서 후보의 매칭 이유**, 보고서 내보내기 세 장면을 자세히 보겠습니다.  
> 이제 다음 장의 버튼을 눌러 실제 데모 페이지로 이동하겠습니다.

### 캡처 레퍼런스 — 캡처 없음

- 실제 화면 대신 클릭 순서 아이콘 6개를 사용합니다.
- `대분류 → 실제 팀 → 새싹이 → 긴급 민원/갭 진단 → 통합 정책 근거 → 보고서`
- 마지막에 배포 링크 `https://ukkkk-two.vercel.app/`를 크게 표시합니다.

---

## Slide 16. 라이브 데모 — 보육·돌봄 인프라 → 가족건강팀 20건

### PPT 실행 화면

- 큰 버튼: **`UKKKK 라이브 데모 열기`**
- 버튼 아래 URL: `https://ukkkk-two.vercel.app`
- 보조 문구: `보육·돌봄 인프라 179건 → 가족건강팀 20건`
- 발표자는 이 장에서 실제 페이지를 연 뒤 아래 큐시트 순서로 시연

### 105초 시연 큐시트

| 시간 | 발표자 클릭 | 화면에서 보여줄 결과 | 발표 멘트 |
|---:|---|---|---|
| 0–8초 | 홈에서 상단 필터 초기화 | 전체 R&R 부서 | “먼저 전체 상태에서 시작합니다.” |
| 8–18초 | 첫 번째 필터에서 `보육·돌봄 인프라` 선택 | 담당군 **179건**, 실제 관련 팀 후보만 표시 | “정책 분류 기준으로 먼저 179건의 업무 범위를 잡습니다.” |
| 18–28초 | 두 번째 필터에서 `가족건강팀` 선택 | 총 분석 제안 **20건** | “실제 R&R 팀을 더해 관련 후보가 포함된 20건으로 좁힙니다.” |
| 28–52초 | 좌측 10개 메뉴를 순서대로 한 번씩 클릭 | 현황·키워드·긴급·정책·사각지대·자치구·갭·로그·통합근거·필터 검증이 동일 범위로 갱신 | “같은 20건을 수요, 공급, 지역, 통합 근거, 책임, 품질의 서로 다른 질문으로 확인합니다.” |
| 52–68초 | `정책 갭 진단`에서 군집 선택 | 문제 군집·우선점수·진단 매트릭스·상세 근거 재구성 | “전체 57개가 아니라 현재 범위에서 먼저 볼 문제와 근거를 좁힙니다.” |
| 68–82초 | `긴급 민원 처리` 이동, 카드 상세 펼치기 | 본문·공식 URL·주관 1순위·협조 2·3순위 표시 | “요약에서 멈추지 않고 원문과 책임 후보를 함께 검증합니다.” |
| 82–92초 | 현재 페이지 하단의 새싹이 클릭 | 현재 페이지·보육·돌봄 인프라·가족건강팀 기준 안내 | “길잡이는 현재 화면과 두 단계 선택 범위를 읽어 다음 행동을 안내합니다.” |
| 92–105초 | 우측 상단 `보고서 다운로드` | 선택 범위가 반영된 상세·간부·외부공유 보고서 | “같은 범위를 목적별 보고서와 CSV로 넘깁니다.” |

### 시연 중 실제 대본

> 전체 상태에서 시작하겠습니다.  
> 첫 번째 필터에서 보육·돌봄 인프라를 선택하면 분석 가능한 179건의 정책 범위가 잡힙니다.  
> 두 번째 필터에서 실제 가족건강팀을 선택하면 이 팀이 주관 또는 협조 후보로 포함된 20건으로 좁혀집니다.  
> 이제 수요 현황, 시민 목소리, 긴급 민원, 현행 정책, 정책 사각지대, 자치구 통계, 정책 갭, 결측치·로그, 통합 정책 근거 분석, 필터 검증 통계를 차례로 열겠습니다. 화면마다 질문은 달라도 상단의 20건 범위는 유지됩니다. 통합 정책 근거 분석에서는 같은 안건과 연결된 시민제안·민원·정책·뉴스·통계를 한곳에서 교차 확인하고, 필터 검증 통계에서는 현재 20건의 분류·연도·답변·R&R 분포와 드릴다운 목록을 확인합니다.  
> 정책 갭 진단에서는 전체 57개 군집을 그대로 보여주지 않고 현재 업무 범위의 문제 군집, 우선점수, 매트릭스와 상세 근거를 다시 구성합니다. 이 점수는 자동 결정값이 아니라 먼저 검토할 대상을 좁히는 보조 지표입니다.  
> 긴급 민원 처리에서는 상세 본문과 공식 원문을 확인하고, 가족건강팀이 어느 R&R 순위로 제시됐는지와 2·3순위 협조 후보를 함께 검토합니다.  
> 현재 페이지 하단의 새싹이를 누르면 지금 보고 있는 화면의 기능과 함께 보육·돌봄 인프라 담당군, 가족건강팀 기준의 다음 확인 순서를 안내합니다. 마지막으로 보고서 다운로드를 누르면 지금 선택한 범위가 상세형, 간부 브리핑, 외부 공유용과 CSV에 그대로 반영됩니다.

### 데모 직전 체크

- 첫 번째 필터가 `전체 대분류 담당군`으로 보이는지 확인
- 두 번째 필터가 `전체 실제 R&R 팀`으로 보이는지 확인
- 브라우저 확대율 80~90%, 해상도 1920×1080 권장
- 팝업과 보고서 모달이 화면 밖으로 잘리지 않는지 확인
- 새로 연 브라우저 탭에서 콘솔 오류가 없는지 확인
- 라이브 시연 전 `보육·돌봄 인프라 179건 → 가족건강팀 20건` 재확인
- 인터넷이 끊겨도 캐시된 대시보드 탐색은 가능하지만, Gemini 초안 생성은 대체 초안으로 동작할 수 있음을 인지

### 30초 백업 시연

라이브 환경이 불안정하면 다음 세 장면만 보여줍니다.

1. `보육·돌봄 인프라 → 가족건강팀 → 20건`
2. 제안 상세의 주관 1순위·협조 2·3순위
3. 보고서 다운로드 모달의 범위·CSV

백업 멘트:

> 핵심은 세 가지입니다. 정책 대분류와 실제 팀을 분리해 범위를 정확히 잡고, 제안마다 주관·협조 후보와 원문 근거를 확인하며, 그 범위를 그대로 보고서로 넘깁니다.

### 캡처 레퍼런스 — 라이브 실패용 3개 화면

1. `수요 현황 종합`: 선택 범위와 20건 KPI.
2. `정책 갭 진단`: 군집 상세, R&R, 연결 근거.
3. `보고서 다운로드`: 동일 대분류·팀 이름과 20건이 미리보기에 반영된 화면.
4. 세 캡처는 한 장에 축소하지 말고 숨김 백업 슬라이드 3장에 각각 전체 화면으로 준비합니다.

---

## Slide 17. 구현 확인에서 재현 가능한 과업 비교로

### PPT 화면

| 현재 구현에서 재현 가능한 비교 | 확인 방법 |
|---|---|
| 824건 전체 탐색 → 분석 가능 담당군 179건 → 실제 팀 20건 | 같은 필터 적용 전후 후보 수 기록 |
| 메뉴를 바꿔도 20건 범위 유지 | 10개 화면의 헤더·KPI·목록 건수 대조 |
| 원문·공식 URL·R&R 1·2·3순위 한 카드 확인 | 상세 카드에서 필드 존재 여부 점검 |
| 기존 정책·민원·뉴스·통계 근거 연결 | 안건별 연결 출처와 링크 개수 확인 |
| 같은 범위의 3종 보고서·CSV 생성 | 보고서 제목·수치·본문 범위 대조 |
| 오류 제안 → AI·개발 로그 누적 | 레코드 ID·화면·원문·상태 이력 확인 |

### 발표 대본 — 35초

> 성과를 과장하지 않으면서도 구현 효과는 재현 가능한 과업 단위로 확인할 수 있습니다.  
> 이번 시연에서는 824건 전체 목록에서 분석 범위 밖과 원문 미연결을 분리한 뒤, 보육·돌봄 인프라 179건, 가족건강팀 관련 20건으로 후보가 줄어드는 과정을 확인합니다. 이것은 업무시간 절감률이 아니라, 동일한 데이터를 대상으로 사용자가 직접 확인해야 할 후보 수가 얼마나 줄었는지를 보여주는 값입니다.  
> 또한 메뉴 이동 후 건수 유지, **원문과 담당부서 매칭 이유의 동시 표시**, 같은 범위의 보고서 생성 여부를 화면과 데이터로 대조합니다.  
> 향후에는 이 과업 로그를 이용해 클릭 수, 탐색 단계, 오분류 수정률을 반복 측정할 수 있습니다.

### 해석 주의

- 후보 축소율은 시간 절감률이나 추천 정확도가 아님
- R&R 후보는 담당자의 최종 검토·수정 대상
- 지역 복원 후보는 본문 근거가 있을 때만 제시
- 만족도나 실제 행정 성과를 대신하지 않음

---

## Slide 18. 운영형 정책 의사결정 도구로

### PPT 화면

3단계 로드맵:

1. **데이터 운영화**
   - 정기 수집 스케줄
   - 변경 이력·버전·출처 관리
   - 원문 확보율 개선
2. **행정 검증**
   - R&R 정답셋과 담당자 피드백
   - 결측치 복원 성능 측정
   - 공식 보고서 템플릿 검수
3. **서비스 안정화**
   - 대용량 JSON API·DB 분리
   - 권한·감사로그·개인정보 보호
   - 실제 PDF/HWP 문서 생성

### 발표 대본 — 30초

> 운영형 도구로 확장하려면 세 가지 기반이 필요합니다.  
> 먼저 정기 수집과 출처 관리를 구축하고 상세 원문 확보율을 높여야 합니다. 다음으로 R&R 후보와 지역 범위 분류를 평가할 수 있는 정답 기준과 검증 지표가 필요합니다.  
> 마지막으로 대용량 데이터를 API와 데이터베이스로 분리하고, 권한·감사로그·개인정보 보호, 실제 PDF와 HWP 생성까지 연결해야 합니다.

---

## Slide 19. 10개 화면이 남기는 하나의 검토 결과

### PPT 화면

중앙 4단 연결:

`수요` → `공급` → `책임` → `조치`

근거 카드:

- 수요: 현황·시민 목소리·긴급 민원
- 공급·지역: 현행 정책·사각지대·자치구 통계
- 통합 근거: 시민제안·민원·정책·뉴스·통계와 출처
- 책임: Gap 5축·R&R 1·2·3순위·원문 URL
- 조치: 답변 초안·3종 보고서·CSV·AI 개발 로그

새싹이 말풍선:

“모르는 것은 표시하고, 근거는 연결하고, 결정은 사람이 합니다.”

### 발표 대본 — 25초

> 오늘 확인한 열 개 화면은 각각 다른 기능을 나열한 것이 아닙니다. 현황·시민 목소리·긴급 민원에서 수요를 발견하고, 현행 정책·사각지대·자치구 통계에서 공급과 지역 조건을 대조합니다. 통합 정책 근거 분석에서 시민제안·민원·정책·뉴스·통계를 교차 확인한 뒤, 갭 진단에서 우선순위와 R&R 후보를 검토합니다. 마지막으로 답변 초안과 목적별 보고서, CSV, 개선 로그로 다음 조치를 남깁니다.  
> 유케이는 이렇게 시민 제안을 **수요에서 책임으로, 근거에서 조치로 연결해 상상대로 서울의 후속 활용을 돕는 백오피스**입니다. 모르는 값은 표시하고, 근거는 연결하며, 최종 결정은 담당자가 합니다. 새싹이와 함께한 유케이의 여정이었습니다. 감사합니다.

---

## Appendix A. 통합 스키마와 분석 결과 정의

### PPT 화면

- 입력 피처: 건수·공감·댓글·답변·날짜·본문·지역·정책·뉴스·통계
- 분석 결과: 8대 대분류·중소분류, 57개 문제 군집, Gap 5축, R&R 1·2·3순위 후보
- 최종 판단: 담당자가 원문과 업무분장 근거를 대조해 수정·승인·반려

### 발표 대본

> 부록에서는 데이터분석 정의서의 구조에 맞춰 입력 피처, 전처리·분류 결과, Gap과 R&R 출력, 최종 판단 주체를 구분했습니다. 현재 엔진은 정답을 예측하는 단일 감독학습 모델이 아니라 NLP·규칙·가중치 기반의 분류·군집·우선순위·후보 추천 구조입니다.

---

## Appendix B. 텍스트 전처리와 모델별 파싱 구조

### PPT 화면

| 처리 단계 | 입력·파싱 방식 | 출력 |
|---|---|---|
| 공통 전처리 | 조사·접미사 제거 → 도메인 유사어 통합 → 불용어 제거 | 정규화 토큰 |
| 계층 분류 | 제목·본문·원 카테고리의 규칙 키워드 점수와 원 분류 부스트 | 8대·중·소분류, 분류 신뢰도 |
| 의미 분류 | `snunlp/KR-SBERT-V40K-klueNLI-augSTS` 임베딩과 코사인 유사도 | 의미 카테고리, 유사도 점수 |
| 유사 제안 군집 | KR-SBERT 임베딩을 계층적 군집화(`cosine`, `average`, 거리 `0.25`) | `cluster_id`, `cluster_size` |
| 뉴스 파싱 | 규칙 분류 → 기존 8대 분류 → 수집 카테고리 → NMF 토픽 맵 순으로 보완 | 뉴스 대·중·소분류, 근거, 신뢰도 |
| 지역·R&R | 자치구 키워드와 모호어 감점 / 업무분장과 본문 토큰 가중치 대조 | 지역 후보·점수 / 주관 1순위·협조 2·3순위 |

새싹이 말풍선:

**“한 모델이 모두 결정하지 않아요. 단계마다 입력과 출력, 근거를 나눠 확인할 수 있어요.”**

### 발표 대본

> 텍스트 처리는 한 모델이 모든 값을 한 번에 예측하는 구조가 아닙니다. 먼저 한국어 조사와 접미사, 불용어를 제거하고 도메인 유사어를 통합합니다.  
> 계층 분류는 제목·본문·원 카테고리의 키워드 점수로 8대·중·소분류와 신뢰도를 만들고, KR-SBERT는 카테고리 설명과의 코사인 유사도로 의미 분류를 보완하며 유사 제안을 군집화합니다. 뉴스는 규칙 결과가 없을 때 기존 분류, 수집 카테고리, 이미 생성된 NMF 토픽 라벨을 순서대로 매핑합니다.  
> 마지막으로 자치구는 본문 키워드와 모호어 감점을 사용해 후보와 점수를 남기고, 실제 팀은 업무분장 원문과 제안 텍스트를 가중치로 대조합니다. 따라서 각 결과가 어떤 입력과 규칙에서 나왔는지 단계별로 추적할 수 있습니다.

### 코드 근거

- `docs/데이터분석정의서_ver3.md`
- `frontend/src/utils/textMining.ts`
- `scripts/테스트용/classify_policy_and_match_staff.py`
- `scripts/테스트용/04_semantic_grouping.py`
- `scripts/테스트용/classify_naver_news_taxonomy.py`
- `scripts/06_build_department_ranking.py`

---

## Appendix C. 두 단계 필터와 R&R 후보 모델

### PPT 화면

| 정의서 항목 | 현재 구현 |
|---|---|
| 목적 | 정책 분류와 실제 조직을 분리해 필터 의미와 R&R 후보를 명확히 함 |
| 입력 | 제안 카테고리·제목·본문·기존 담당 후보 + 업무분장 원천 18개 행 |
| 전처리 | 정책 카테고리를 8대 담당군으로 정규화, 본문 토큰과 업무분장 키워드 대조 |
| 모델 | 카테고리·업무 일치 `+30`, 업무 키워드 `+10`, 기존 담당 후보 `+50`의 규칙·가중치 랭킹 |
| 출력 | 1순위 주관 후보, 2·3순위 협조 후보, 연락처·업무 요약·매칭 사유 |
| 검색 규칙 | 전체+팀과 특정 담당군+팀 모두 1·2·3순위 후보를 포함하되, 특정 담당군에서는 해당 정책 범위와 교차 |
| 한계 | 공식 배정이나 학습된 정확도 아님. 일반 단어 오매칭은 문맥 예외 규칙으로 제외 |

새싹이 말풍선:

**“정책 분류와 조직 이름을 분리해야 같은 팀이 여러 분야에 등장해도 검색 의미가 흔들리지 않아요.”**

### 발표 대본

> 이 페이지가 두 필터를 나눈 이유와 실제 모델입니다. 첫 번째 결과는 제안을 8개 정책 영역으로 묶는 분류이고, 두 번째 결과는 업무분장 원천 18개 행과 제안 텍스트를 대조해 만드는 R&R 후보 순위입니다. 현재 화면 드롭다운에 실제 등장하는 팀은 11개이며, 선택지는 현재 제안 데이터에서 동적으로 구성됩니다.  
> 카테고리 업무 일치에는 30점, 본문과 업무분장의 키워드 일치에는 단어별 10점, 기존 담당 후보 일치에는 50점을 주고 상위 세 팀을 정렬합니다. 장애·임신건강·직원복지처럼 일반 단어로 잘못 연결되기 쉬운 팀은 해당 문맥이 없을 때 제외합니다.  
> 이 순위는 공식 배정이 아니라 검토 후보입니다. 화면에서는 전체 담당군일 때 1·2·3순위 관련 안건을 찾고, 특정 담당군을 먼저 고른 경우 그 정책 범위와 실제 팀의 1·2·3순위 후보를 교차해 보여줍니다.

### 코드 근거

- `scripts/06_build_department_ranking.py`
- `frontend/src/utils/departments.ts`
- `frontend/src/App.tsx`

---

## Appendix D. 로드맵 요구사항과 확인 가능한 산출물

### PPT 화면

- 시스템 기획·PRD
- 데이터분석 정의서
- 발표 대본·라이브 시연
- React·TypeScript·Vite 프론트엔드
- Python 데이터 동기화·정합성 검사
- Vercel 배포와 Gemini 초안·템플릿 Fallback

### 발표 대본

> 마지막 부록에는 로드맵의 기획, 데이터 분석, 구현, 검증, 배포 요구사항을 실제 확인 가능한 산출물과 연결했습니다. 별도 파일로 확인되지 않는 산출물이나 측정하지 않은 성과는 완료 사실로 제시하지 않습니다.

---

# 4. PPT 제작 체크리스트

## 4-1. 반드시 새로 캡처할 화면

기존 발표 이미지에는 과거의 단일 부서 필터와 오래된 화면 구성이 남아 있으므로 다음 화면은 최신 버전으로 다시 캡처합니다.

1. 상단의 `전체 대분류 담당군` + `전체 실제 R&R 팀`
2. `보육·돌봄 인프라` 선택 후 179건
3. `가족건강팀` 선택 후 홈 20건
4. 시민 목소리 분석 20건
5. 제안 상세의 주관 1순위·협조 2·3순위
6. 새싹이의 현재 페이지·가족건강팀 맞춤 안내
7. 보고서 다운로드 모달과 외부 공유용 미리보기
8. 결측치 복원 화면의 804건 미상 현황
9. 정책 갭 진단의 57개 군집·가중치 모델
10. 자치구 통계 비교의 3개 하위 화면

## 4-2. 기존 이미지를 그대로 쓰지 말아야 하는 이유

- `slide_3-1_map_default.png`, `slide_3-1-1_map_clicked.png`는 홈 중심의 과거 흐름입니다.
- 현재 홈은 수요 현황 종합이고 지도는 자치구 통계 비교 메뉴에 있습니다.
- 과거 화면의 단일 부서 선택은 현재 2단계 담당군·실제 팀 구조와 다릅니다.
- 과거 제안 카드에는 본문 대체와 R&R 순위 표시 오류가 포함될 수 있습니다.

## 4-3. 시각 디자인 원칙

- 슬라이드당 핵심 문장 1개, 숫자 1개, 그림 1개를 우선합니다.
- 표는 최대 6행, 본문은 3줄 이내로 축약합니다.
- 대분류 담당군은 태그 아이콘, 실제 팀은 건물 아이콘으로 일관되게 구분합니다.
- 주관 1순위는 파란색, 협조 2·3순위는 회색으로 통일합니다.
- 새싹이는 안내자 역할만 하고 핵심 데이터나 버튼을 가리지 않습니다.
- 화면 캡처에는 실제 개인 식별정보나 API 키를 노출하지 않습니다.

---

# 5. 예상 질문과 답변

## Q1. 8개 담당군이 실제 서울시 부서입니까?

> 아닙니다. 첫 번째 선택기의 8개 담당군은 정책 수요를 묶는 대분류입니다. 실제 조직과 업무분장은 두 번째 실제 R&R 팀 선택기와 제안별 1·2·3순위 카드에서 별도로 보여줍니다.

## Q2. 한 팀이 여러 담당군에 왜 겹칩니까?

> 출산·양육 정책은 대상과 업무가 겹치기 때문에 한 팀이 여러 분야의 주관 또는 협조 후보가 될 수 있습니다. 중복을 제거하지 않고, 전체 담당군에서는 1·2·3순위 관련 안건을 찾으며 특정 담당군에서는 그 정책 범위와 팀 후보를 교차해 보여줍니다.

## Q3. R&R 추천이 틀리면 어떻게 합니까?

> 자동 확정이 아니라 업무분장 근거를 제공하는 후보 랭킹입니다. 담당자는 업무 요약과 전화번호, 주관·협조 순위를 확인하고 최종 판단합니다. 오매칭 신고와 승인 이력은 통합 로그에 남깁니다.

## Q4. 본문이 없는 제안은 어떻게 처리합니까?

> 제목을 상세 본문처럼 반복하지 않습니다. 현재 원문 연결 실패 16건은 `원문 확인 필요`로 표시하고 공식 URL과 수집 상태를 확인합니다. 원문과 저장 내용이 동일한 짧은 제안은 정상 원문으로 승인할 수 있습니다.

## Q5. 원본 자치구 미상 804건을 모두 결측치로 봅니까?

> 아닙니다. 원본 자치구 미상에는 서울시 전체에 대한 요구와 실제 지역 범위 판별 불가가 함께 포함됩니다. 원본 특정 자치구 20건, 본문 자치구명 복원 후보 49건, 서울시 전체 명시 5건을 구분하고, 나머지 750건은 지역 범위 미확인으로 유지합니다. 자치구 비교는 공공통계를 중심으로 보고 지역이 확인된 제안만 보조 근거로 사용합니다.

## Q6. 정말 실시간입니까?

> 현재 데모는 안정적인 재현을 위해 수집·정제한 캐시 데이터를 사용합니다. 화면의 필터링과 지표 재계산은 즉시 반영됩니다. 실제 운영 단계에서는 정기 수집 API, DB, 버전 관리가 추가로 필요합니다.

## Q7. AI가 답변을 자동 발송합니까?

> 아닙니다. Gemini 호출이 가능한 환경에서는 초안을 생성하고, 그렇지 않으면 대체 템플릿을 사용합니다. 어떤 경우에도 담당자가 검토·수정·승인하며 자동 발송하지 않습니다.

## Q8. HWP와 PDF를 바로 받을 수 있습니까?

> 현재 HWP는 행정 문체 텍스트 파일, PDF는 미리보기·인쇄용 요약 흐름, 원자료는 CSV입니다. 네이티브 HWP와 PDF 바이너리 생성은 운영화 단계의 과제입니다.

## Q9. 왜 57개 군집입니까?

> 개별 제안·민원을 정책 문제 단위로 비교하기 위해 현재 진단 데이터에서 생성된 문제 군집 수입니다. 군집 수는 고정된 행정 분류가 아니라 데이터와 군집 기준이 바뀌면 다시 검증해야 하는 분석 단위입니다.

## Q10. 도입 효과는 검증됐습니까?

> 기능 동작과 데이터 정합성은 확인했지만, 실제 업무시간 단축과 R&R 정확도는 검증하지 않았습니다. 따라서 시간 절감률이나 추천 정확도를 성과로 제시하지 않고, 현재 발표 범위를 기능 구현과 데이터 처리 기준으로 한정합니다.

---

# 6. 발표 전 최종 리허설 체크리스트

## 데이터

- [ ] 시민 제안 824건
- [ ] 국민신문고 582건
- [ ] 공식 정책 322건
- [ ] 뉴스 1,145건
- [ ] 정책 갭 진단 57개
- [ ] 분석·정책 대조 가능 730건 / 분석 범위 밖 78건 / 원문 확인 필요 16건
- [ ] 원본 특정 자치구 20건 / 본문 자치구 후보 49건 / 서울시 전체 명시 5건 / 지역 범위 미확인 750건
- [ ] `보육·돌봄 인프라` 179건
- [ ] `보육·돌봄 인프라 → 가족건강팀` 20건

## 기능

- [ ] 전체 담당군 + 실제 팀은 1·2·3순위 관련 안건 검색
- [ ] 특정 담당군 + 실제 팀은 해당 정책 범위와 1·2·3순위 후보 교차 검색
- [ ] 첫 필터 변경 시 두 번째 팀 후보가 유효 목록으로 재구성
- [ ] 홈 외 시민 목소리·긴급 민원·사각지대·통계·갭·결측 화면에도 범위 반영
- [ ] 제안 상세 본문과 공식 URL 정상
- [ ] 새싹이 10개 화면 안내와 현재 범위 안내 정상
- [ ] 보고서 다운로드 버튼 우측 배치
- [ ] 보고서 모달에 현재 범위 반영

## 발표 환경

- [ ] 데모 URL 사전 접속
- [ ] 브라우저 캐시·로그인·팝업 상태 확인
- [ ] 새 탭에서 콘솔 오류 확인
- [ ] 발표 화면 해상도 확인
- [ ] 새싹이가 버튼이나 차트를 가리지 않는지 확인
- [ ] 최신 화면 캡처를 PDF 또는 PPT에 백업
- [ ] 30초 백업 시연 순서 암기
- [ ] 발표 타이머 10분 리허설 2회

---

# 7. 산출물과 코드 근거

| 항목 | 경로 | 발표에서 확인할 내용 |
|---|---|---|
| 발표 대본 | `docs/발표자료/발표_대본_PPT구성_ver14.md` | 10분 대본·시연 큐시트 |
| 1차 발표덱 | `docs/발표자료/UKKKK_발표덱_1차본.pptx` | 기존 디자인·슬라이드 구성 참고 원본 |
| 시스템 기획·PRD | `docs/SYSTEM_DESIGN_PRD_ver3.md` | 문제 정의·기능 요구사항·Human-in-the-loop 승인 구조 |
| 데이터분석 정의서 | `docs/데이터분석정의서_ver3.md` | 데이터셋·EDA·가중치·결측 처리·모델링 명세 |
| EDA 분석보고서 | `docs/EDA_분석보고서_ver1.md` | 최신 수치·분포·품질 상태·필터 검증 |
| 역할·일정 | `docs/역할분담_및_일정가이드.md` | 7/21~7/23 수집·EDA·모델링·MVP 통합 과정 |
| 메인 화면 | `frontend/src/App.tsx` | 10개 화면과 공통 필터 전달 |
| R&R 규칙 | `frontend/src/utils/departments.ts` | 담당군·실제 팀·주관/협조 검색 의미 |
| 시민 제안 정본 | `frontend/src/data/mongttang.json` | 824건 제안 |
| 데이터 동기화 검사 | `scripts/sync_proposal_data.py` | URL·본문·R&R 정합성 점검 |
| 시민 목소리 분석 | `frontend/src/components/CategoryDemand.tsx` | 키워드·다차원 필터·CSV |
| 긴급 민원 처리 | `frontend/src/components/PriorityDetails.tsx` | 원문·R&R·그룹/목록·일괄 답변 |
| 공식 정책 검색 | `frontend/src/components/MongttangList.tsx` | 몽땅정보통 322개 사업 |
| 정책 사각지대 | `frontend/src/components/ClusterVolumeMap.tsx` | 반복 수요 군집 |
| 정책 갭 진단 | `frontend/src/components/GapMatrixDashboard.tsx` | 57개 군집·가중치·승인 |
| 결측치·로그 | `frontend/src/components/MissingDataSimulator.tsx` | 지역 추정·검토·통합 로그 |
| 새싹이 | `frontend/src/components/OfficeAssistant.tsx` | 화면·담당 범위별 업무 안내 |
| 보고서 | `frontend/src/components/ReportExportModal.tsx` | 범위별 미리보기·HWP 텍스트·CSV |
| 텍스트 정규화 | `frontend/src/utils/textMining.ts` | 불용어·유사어·지역 추정 |
| 학술 근거 | `docs/발표자료/논문근거_실증흐름.md` | 발표 전 원문·서지 재검증 |

---

# 8. 로드맵 반영 부록

## 8-1. 프로젝트 로드맵과 실제 산출물 연결

| 로드맵 단계 | 실제 수행·산출물 | 발표 위치 |
|---|---|---|
| 문제·목표·가설 정의 | 정책 수요와 책임·근거·조치 연결이라는 제품 가설, 가상 페르소나 과업 | Slide 0~4 |
| 논문·방법론 조사 | 6개 학술·정책 근거와 3개 가중치 방법론 | Slide 8, Appendix, `논문근거_실증흐름.md` |
| 데이터 수집·스키마 | 5개 출처 수집, 공통 제안·분류·R&R·출처 필드 정의 | Slide 6 |
| 전처리·EDA | ID·본문·날짜·상태 정규화, 한국어 유사어/불용어 처리, 결측률과 자치구 통계 분석 | Slide 6, 14, `데이터분석정의서_ver3.md`, `EDA_분석보고서_ver1.md` |
| KPI·임계값 | 수요·공백·시급성·실행성·근거 신뢰도 5축과 3개 가중치 모델 | Slide 8 |
| 모델링 | 계층 분류, 유사 제안 군집, 뉴스 토픽 파싱, 지역·R&R 후보 랭킹, Gap 우선순위 | Slide 7~8, Appendix A~C |
| 대시보드 설계 | KPI를 10개 화면·공통 필터·상세 패널·보고서로 연결 | Slide 9~12 |
| PRD·기술 구현 | React/Vite 프론트, 캐시 JSON, Python 동기화 검사, Gemini 초안/Fallback | Appendix, 산출물 표 |
| MVP 통합 검증 | 데이터 정합성·필터 연동·10개 화면·빌드 검사 | Slide 17 |
| 라이브 E2E | 담당군→실제 팀→10개 화면→통합 근거→Gap→R&R→새싹이→보고서 | Slide 15~16 |

## 8-2. EDA에서 확인한 실제 데이터 상태

| 확인 항목 | 현재 값 | 제품 설계에 반영한 결정 |
|---|---:|---|
| 시민 제안 | 824건 | 전체 정책 수요 분석 모수 |
| 답변 완료 / 미답변 | 64 / 760건 | 답변 여부를 우선순위 신호 중 하나로 사용 |
| 분석·정책 대조 가능 | 730건 | 분류·군집·정책 대조·R&R 후보 분석 |
| 분석 범위 밖 | 78건 | 출산·양육 분석 모수와 분리 |
| 원문 확인 필요 | 16건 | 원문 대조 후 정상 승인 또는 재수집 검토 |
| 원본 특정 자치구 | 20건 | 원 데이터의 명시값만 확정 지역으로 사용 |
| 본문 자치구 후보 / 서울시 전체 명시 | 49 / 5건 | 직접 등장한 표현만 후보 또는 전체 범위로 구분 |
| 지역 범위 미확인 | 750건 | 서울시 전체와 판별 불가를 임의 분리하지 않음 |
| 정책 갭 분석 단위 | 전체 기준 57개 군집 | 개별 제안이 아닌 반복 문제 단위로 검토 |

발표 문장:

> EDA에서 가장 중요했던 결과는 지역값 `미상`을 모두 결측치로 취급할 수 없다는 점입니다. 원본 특정 자치구는 20건이고, 본문에 자치구명이 직접 등장하는 후보 49건과 서울시 전체 명시 5건을 분리했습니다. 나머지 750건은 전체 시 요구와 판별 불가가 섞여 있어 지역 범위 미확인으로 유지하고, 지역 비교는 공공통계를 중심으로 설계했습니다. 콘텐츠 품질은 `reviewable` 730건, `out_of_scope` 78건, `source_missing` 16건으로 분리하며, 짧은 원문 자체는 결측으로 확정하지 않습니다.

## 8-3. 피처·분석 결과·검증 대상

| 구분 | 정의 |
|---|---|
| 입력 피처 | 제안·민원 건수, 공감·댓글, 답변 여부, 등록일, 본문 키워드, 정책 분류, 지역, 정책·뉴스·통계 근거 |
| 분석 결과 | 정책 대·중·소분류, 유사 문제 군집, Gap 5축 점수, 검토 우선순위, R&R 1·2·3순위 후보 |
| 사용자 출력 | 키워드·차트, 긴급 후보, 근거 패널, 답변 초안, 범위별 보고서·CSV |
| 최종 결정 | 담당자가 원문과 업무분장을 대조해 수정·승인·반려 |
| 후속 검증 | 군집 안정성, R&R 후보 정확도·수용률, 임계값 민감도, 보고서 수정률, 과업 시간·오류·만족도 |

> 현재 구현은 정답 라벨을 예측하는 감독학습 모델이 아니라, NLP·규칙·가중치 기반의 **분류·군집·우선순위·R&R 후보 추천 엔진**입니다. 따라서 발표에서도 ‘정책 자동 예측’보다 ‘검토 범위를 좁히는 의사결정 지원’으로 설명합니다.

## 8-4. KPI와 임계값 표현 원칙

- `30/25/25/10/10`은 기본 균형형 가중치입니다.
- 박미경(2022)형은 시민 수요를, KICCE(2023)형은 정책 공급 공백·지역 형평성을 상대적으로 강조합니다.
- 현재 가중치와 긴급 기준은 문헌과 제품 가설을 바탕으로 설정한 **운영 규칙**입니다.
- 실제 담당자 정답셋으로 최적화된 정확도나 정책 효과로 표현하지 않습니다.
- 발표 후 동일 데이터에 여러 임계값을 적용해 대상 건수와 순위 변화를 비교하는 민감도 분석이 필요합니다.

## 8-5. 기술 스택과 제작 도구

| 영역 | 실제 확인된 구성 |
|---|---|
| 프론트엔드 | React + TypeScript + Vite |
| 시각화·인터랙션 | Recharts, Motion, Lucide 아이콘 |
| 데이터 제공 | 수집·정제된 JSON 캐시와 공공통계 데이터 |
| 데이터 검사 | Python 동기화·정합성 검사 스크립트 |
| 생성형 AI | Gemini 답변 초안, 키 미설정·호출 실패 시 템플릿 Fallback |
| 배포 | Vercel |
| 상태·피드백 | 화면 상태와 localStorage 기반 승인·오매칭·복원 로그 |

제작 도구 표기 원칙:

- Stitch와 Antigravity는 제품 성능이나 분석 방법론이 아니라 **UI 탐색·구현 보조 도구**로만 소개합니다.
- Stitch 산출물은 실제 와이어프레임 파일 또는 캡처가 확인될 때만 발표 자료에 표시합니다.
- Antigravity는 프롬프트 또는 구현 이력이 확인되는 범위에서만 표기하며, 현재 엔진을 감독학습 ‘예측 모델’로 과장하지 않습니다.
- Gemini가 제안한 방법론은 코드·데이터·원 논문으로 다시 대조한 뒤 근거로 사용합니다.

산출물 확인 주의:

- 역할·일정 문서에는 `analysis/eda_analysis.ipynb`가 완료 산출물로 적혀 있으나 현재 저장소에서 해당 파일을 확인하지 못했습니다.
- 별도 EDA 노트북은 확인되지 않지만, 독립 산출물인 `EDA_분석보고서_ver1.md`에 최신 코드·데이터 수치와 분포·품질 상태를 정리했습니다.

## 8-6. 프로젝트 기간과 기준일 구분

- 개발 스프린트: 로드맵 기준 **7월 20일~23일**
- 발표·보완 작업: 스프린트 이후 화면·데이터·대본 정합성 보완
- 대시보드 표시 기준일: **2026년 7월 25일**

> 개발 기간과 데이터 기준일은 서로 다른 값입니다. 발표에서는 “7월 20일부터 23일까지 MVP를 구축했고, 7월 25일 기준 데이터와 화면으로 보완했다”고 구분합니다.

---

# 9. 발표자 한 문장 요약

> **유케이는 5개 출처의 출산·양육 데이터를 8대 정책 담당군과 실제 R&R 팀으로 연결해, 담당자가 반복 수요를 찾고 근거를 검토하고 다음 조치를 보고서로 넘기도록 돕는 Human-in-the-loop 정책 의사결정 대시보드입니다.**
